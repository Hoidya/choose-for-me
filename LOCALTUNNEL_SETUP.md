# 🌐 Localtunnel 设置指南

使用 Localtunnel 可以让其他人通过公网访问你的本地开发服务器。

## 安装 Localtunnel

```bash
npm install -g localtunnel
```

或者使用 npx（不需要全局安装）：

```bash
npx localtunnel --port 3000
```

## 使用方法

### 方法 1: 基本使用

1. **启动 Next.js 开发服务器**
   ```bash
   npm run dev
   ```
   确保服务器在 `http://localhost:3000` 运行

2. **在另一个终端窗口启动 Localtunnel**
   ```bash
   npx localtunnel --port 3000
   ```

3. **你会看到一个公网 URL**
   ```
   your url is: https://random-name.loca.lt
   ```

4. **分享这个 URL**
   - 其他人可以通过这个 URL 访问你的应用
   - URL 在每次启动时会变化（除非使用 `--subdomain`）

### 方法 2: 使用固定子域名（推荐）

```bash
npx localtunnel --port 3000 --subdomain choose-for-me
```

这样你会得到一个固定的 URL：
```
https://choose-for-me.loca.lt
```

**注意：** 子域名可能已被占用，如果失败，尝试其他名称。

### 方法 3: 创建便捷脚本

在 `package.json` 中添加脚本：

```json
{
  "scripts": {
    "dev": "next dev",
    "tunnel": "lt --port 3000 --subdomain choose-for-me",
    "dev:tunnel": "concurrently \"npm run dev\" \"npm run tunnel\""
  }
}
```

然后安装 concurrently（如果需要同时运行）：
```bash
npm install --save-dev concurrently
```

使用：
```bash
npm run dev:tunnel
```

## 注意事项

### 1. 安全性
- Localtunnel 是公开的，任何人都可以访问
- 不要在生产环境使用
- 仅用于开发和演示

### 2. 性能
- 免费版本可能有速度限制
- 连接可能不稳定
- 适合临时分享和测试

### 3. 防火墙
- 确保防火墙允许连接
- 某些网络可能阻止 Localtunnel

### 4. 环境变量
- 确保 `.env.local` 中的 Supabase 配置正确
- 公网访问时，Supabase 需要允许你的域名（通常不需要额外配置）

## 替代方案

如果 Localtunnel 不工作，可以考虑：

1. **ngrok** - 更稳定，但需要注册
   ```bash
   npm install -g ngrok
   ngrok http 3000
   ```

2. **Cloudflare Tunnel** - 免费且稳定
   ```bash
   npm install -g cloudflared
   cloudflared tunnel --url http://localhost:3000
   ```

3. **部署到 Vercel** - 永久解决方案
   - 参考 README.md 中的部署说明

## 快速开始

```bash
# 终端 1: 启动开发服务器
npm run dev

# 终端 2: 启动 Localtunnel
npx localtunnel --port 3000 --subdomain choose-for-me
```

然后分享 URL 给其他人即可！

## 故障排除

### 问题：连接被拒绝
- 确保 Next.js 服务器正在运行
- 检查端口是否正确（默认 3000）

### 问题：子域名已被占用
- 尝试其他子域名
- 或者不使用 `--subdomain` 参数

### 问题：页面无法加载
- 检查 Next.js 服务器是否正常运行
- 查看浏览器控制台的错误信息
- 确认 Supabase 配置正确

